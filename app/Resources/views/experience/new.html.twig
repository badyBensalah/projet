{% extends 'experienceLayout.html.twig' %}

{% block body %}
    {{ form_start(form) }}
        {{ form_widget(form) }}
    <input class="btn_1 green"  type="submit" value="Add Article" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('experience_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
