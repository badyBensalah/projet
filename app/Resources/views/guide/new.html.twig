{% extends 'guideLayout.html.twig' %}

{% block body %}
    {{ form_start(form) }}
        {{ form_widget(form) }}
    <input class="btn_1 green"  type="submit" value="Add Guide" />
    {{ form_end(form) }}
{% endblock %}
